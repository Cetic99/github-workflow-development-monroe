CREATE TABLE "MoneyStatus" (
	"Id" INTEGER GENERATED BY DEFAULT AS IDENTITY NOT NULL,
	"Key" VARCHAR(2000) NOT NULL,
	"Value" BLOB SUB_TYPE TEXT NOT NULL,
    CONSTRAINT "UQ_MoneyStatus_Key" UNIQUE ("Key")
);
